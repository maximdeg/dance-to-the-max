# Web-first; native mobile deferred

v1 ships as a responsive web app rather than native iOS/Android, even though the spec mentions a mobile app for offline use. This is the fastest, cheapest path to validating willingness-to-pay; native apps (and true offline downloads + native push) come in a later phase once the model is proven. Consequence: the device limit is defined over concurrent browser **Sessions**, not installed apps.
